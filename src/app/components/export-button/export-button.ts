import { Component } from '@angular/core';

/**
 * @deprecated Export functionality is now integrated into WorkspaceSelector
 * and uses the backend's GET /api/workspaces/:id/export endpoint.
 * This component is kept for reference but not wired into any template.
 */
@Component({
  selector: 'app-export-button',
  template: ``,
  styles: ``,
})
export class ExportButton {}
